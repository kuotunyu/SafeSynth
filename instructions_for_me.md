# 換你做 — 02-safesynth-ppe

> 這份檔案只留**需要你親自判斷或執行外部動作**的事。
> 本機可逆的實作、測試與 commit 已授權自動完成；不會自行建立 remote、push 或發佈。
> 最後更新：2026-07-31（M13 pool 依 [ADR-013](docs/decisions.md#adr-013) 重生成之後）

---

## 現在：**看 12 張圖，裁決 1 件事**（約 15 分鐘）

上次你回報的三件事，追下去變成**四個缺陷**。三個修好了，一個只修好一半。

### 你上次回報的，處理結果

| 你說的 | 結果 |
|---|---|
| 「整張圖黑掉」 | **真 bug，已修。** 低光的 gamma×gain 兩個參數相乘，抽到 3.17×0.54 把中灰壓到亮度 16。**38.3% 的接受圖**都中招 |
| 「hard negative 像後製」 | **真 bug，修好一半。** 質感修好了，放置位置沒有——見下方要你裁決的事 |
| 「那顆頭一團黑」（第 01 格） | **不修，如實記錄。** 那顆頭的亮度 32.2，真實 head 的 p1 是 23.19——它落在真實分布內。門檻拉到抓得到它，就會連真實資料裡最暗的裸頭一起丟掉，而稀少的裸頭正是這專案要補強的類別 |
| 「扁 helmet 框不需要」 | **不能刪。** 那是真實資料原本就有的標註（被畫面右緣切掉的安全帽），實驗鐵律禁止刪真實標註，而且 Real-only 組也有它。它上次看不見是因為整張圖黑掉，那個根因已修 |

**另外兩個是我自己查出來的**（你沒看到，因為被上面那兩個蓋住了）：
- 頭的尺寸沒有繼承被取代的安全帽，貼出 **52×68** 的頭去取代 **24×30** 的安全帽
- 過濾器被自己的調和器繞過：素材亮度 8.5 的黑剪影被抬到 45.4 就過關了

---

### 要看的圖：全部在 `reports/figures/review/`（那個資料夾只有這 14 個檔案）

**那個資料夾裡有一份 `README.md`，寫了看的順序與每個欄位的意思。**
其他 70 幾個歷史檔案已經移到 `reports/figures/archive/`，不用理。

每張 **2×3 共 6 格、放大兩倍、左上角黃底大編號**，每個情境兩頁（`_p1` / `_p2`）。
標題列這次多了 `min_obj_luma`（該圖最暗標註物件的亮度）與 `lowlight`（低光強度），
所以你如果覺得「看不到」，可以直接跟數字對照，我們就不用各說各話。

| 檔案 | 要看什麼 |
|---|---|
| `preview_head_no_helmet_p1` / `_p2` | 主敘事指標 #2。裸頭**尺寸是否合理**（這次修的重點）、有沒有殘留帽緣 |
| `preview_hard_negative_p1` / `_p2` | **這張要你裁決**，見下 |
| `preview_small_distant_p1` / `_p2` | 主敘事指標 #1。遠處小物件像不像距離造成的，而不是縮小的貼紙 |
| `preview_low_light_blur_p1` / `_p2` | 暗，但**還看得到東西**（上次的災情就在這裡） |
| `preview_partial_occlusion_p1` / `_p2` | 遮擋是否合理 |
| `preview_crowded_p1` / `_p2` | 前後排序是否正確 |

---

### ⚠️ 要你裁決的一件事：hard negative 的**放置位置**

**質感已經修好，這部分有客觀證據**（Laplacian 變異數量測物件表面紋理）：

| | p50 |
|---|---:|
| 真實安全帽表面 | 1350.9 |
| distractor **修前** | **52.4** ← 這就是你說的「像後製」 |
| distractor **修後** | **503.3** |

修前它們是**扁平、無紋理、硬邊的色塊**，因為 distractor 完全沒走標註貼上的
光度管線（羽化、邊緣去汙、Lab 調和、雜訊匹配四樣一樣都沒做）。現在走同一條了，
還加了接地陰影。對照圖：`reports/figures/review/k11_hard_negative_before.png` 對比現在的預覽圖。

**但放置位置我修不動，這點要講清楚。**
那些黃色圓頂仍然可能**落在半空中**——隧道口、天空、人的身體前面。
接地陰影只在「那個位置真的有地面」時才有用，而要知道哪裡有地面需要深度理解。
我試過用資料推導深度先驗：拿 17,815 個真實標註擬合「物件越靠畫面下方越大」，
**R² = 0.0001**，這個資料集根本沒有這個關係。沒有便宜的解法。

**所以請你裁決：這種程度的「浮在半空」能不能接受？**

- **能接受** → 記為已知限制，Phase 2 照常推進。
  代價：這些 distractor 偏 easy negative，
  「hard-negative 每圖誤報數」這個指標測到的東西會比預期弱，README 要寫明
- **不能接受** → 我把 hard_negative 這個情境**整個拿掉**，不生成假的難負樣本。
  代價是少一個目標情境（目前佔 13%），但其餘五個情境不受影響

我的建議是**能接受**——[ADR-011](docs/decisions.md#adr-011) 已經把
「貼上痕跡可被偵測」當成本專案的公開發現，放置真實度是同一個限制的子集，
拿掉一整個情境的代價比留著它大。**但這是你的決定。**

回饋報編號就好：
```
preview_hard_negative_p1 第 03 格：浮得太誇張
preview_head_no_helmet_p2 第 09 格：頭還是太大
裁決：能接受 / 不能接受
```

---

## 接下來會輪到你的兩個時機

### ⏳ 時機 2：Phase 2 的 `M15` → `M16` → **跑一趟 Colab**（預計 4–8 小時等待）

我會先在本機把 notebook 跑通 1-step smoke test，然後給你一份照做就行的清單：
複製到 Drive 的哪個路徑、Runtime 選什麼、需要哪些 Secrets、預估時數與 compute units、
跑完下載哪些檔案放回 `results/colab/` 的哪裡。

你的 Colab 是 **Pro+（500 CU/月，含 24 小時背景執行）**，
估計 8 次訓練用 L4 約 113 CU，額度很夠。

### ⏳ 時機 3：發佈前 → **建 GitHub repo 與 push**（預計 20 分鐘）

依規矩，**建立 remote、push、Hugging Face 上傳一律由你親自執行**，我只準備內容與逐行指令。
到時會先跑個人 skill `publish-repo` 做完整驗收。

---

## 一件現在就可以順手做的事（非必要）

全域 `git config user.email` 目前是你的學校信箱 `03131047@gm.scu.edu.tw`。
本專案三個 repo 的 repo-local 設定都已改成 noreply，但**全域沒動**——
下一個新 repo 還是會中招：

```bash
git config --global user.email "61350295+kuotunyu@users.noreply.github.com"
```

另外 `1_DefectForge` 與 `3_FormosaNLU` 早期的 commit 已經寫進學校信箱，
要清掉得改寫歷史（這是你親自執行的動作）：

```bash
cd "C:/Users/3Hml/Desktop/mySyntheticData/1_DefectForge" && git filter-repo --email-callback 'return b"61350295+kuotunyu@users.noreply.github.com" if b"gm.scu.edu.tw" in email else email' --force
```

本專案（`2_SafeSynth`）從第一筆 commit 就是乾淨的，不需要處理。

---

## 這個專案現在誠實的樣子

值得先知道，因為它會影響你怎麼跟別人介紹這個專案。**有三個限制**：

**1. H4 這道預先註冊的閘門沒有通過。**
貼上痕跡的可偵測度 AUC **0.9053**（95% CI 0.9013–0.9090，106,144 個 patch），
預先登記的上限是 0.60。試了 9 條合成路線與 18 輪 labeler 迭代都翻不過來。
我們的處置**不是**放寬門檻或換個弱一點的分類器來「通過」，
而是把它當成本專案的一項發現照實發表，並把生成量上限壓在 1×（不做 2×）。

**2. copy-paste 在 3,500 張背景上會飽和。**
接受率不是常數——去重要拿新樣本跟**所有已接受的**比，所以 pool 越大接受率越低：
2,000 張候選時 58.4%、10,000 張時 33.8%、14,000 張時 29.8%，
而且 `NEAR_DUPLICATE_SYNTHETIC` 變成最大宗的拒絕原因。
要湊到 1×（3,500 張）需要 14,000 張候選。這是方法的天花板，不是門檻太緊。

**3. hard negative 的放置真實度只修好一半**（見上方要你裁決的那件事）。

三個限制**都可以寫，而且都比假裝沒有誠實。**
細節見 [ADR-011](docs/decisions.md#adr-011) 與 [ADR-013](docs/decisions.md#adr-013)。
